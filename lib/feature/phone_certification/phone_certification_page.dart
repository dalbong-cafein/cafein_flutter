import 'package:flutter/material.dart';

class PhoneCertificationPage extends StatelessWidget {
  const PhoneCertificationPage({
    super.key,
  });

  static const routeName = 'PhoneCertificationPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('PhoneCertificationPage'),
      ),
    );
  }
}
