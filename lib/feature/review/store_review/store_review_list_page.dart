import 'package:flutter/material.dart';

class StoreReviewListPage extends StatelessWidget {
  const StoreReviewListPage({Key? key}) : super(key: key);

  static const routeName = 'StoreReviewListPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
