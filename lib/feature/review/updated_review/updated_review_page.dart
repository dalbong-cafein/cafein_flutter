import 'package:flutter/material.dart';

class UpdatedReviewPage extends StatelessWidget {
  const UpdatedReviewPage({Key? key}) : super(key: key);

  static const routeName = 'UpdatedReviewPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
